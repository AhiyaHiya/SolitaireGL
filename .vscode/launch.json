{
  "version": "0.2.0",
  "configurations": [
    {
        "name": "Debug (local build)",
        "type": "lldb",
        "request": "launch",
        "program": "${workspaceFolder}/build/solitaire",
        "args": [],
        "stopOnEntry": false,
        "cwd": "${workspaceFolder}",
        "preLaunchTask": "cmake: build only"   // or just run nix develop first
    }
  ]
}