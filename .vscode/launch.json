{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug C++ (LLDB)",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/build/solitaire",
      "args": [],
      "cwd": "${workspaceFolder}",
      "stopOnEntry": false,
      "sourceLanguages": ["cpp"],
      "preLaunchTask": "build"
    }
  ]
}